name: Web
on: [push]

jobs:
  auto-deploy:
    runs-on: netlxc
    steps:
      - name: pull
        uses: actions/checkout@v4
      - name: sync-uv
        run: uv sync
      - name: deploy
        run: | 
          chmod +x ./run.sh
          uv run ./run.sh